#include <stdio.h>
#include "platform.h"
#include "xil_printf.h"
#include "xparameters.h"
#include "xaxidma.h"

int main()
{
    init_platform();

    print("Hello World\n\r");
    XAxiDma dmaInstance;
    XAxiDma_Config *dmaConfig = XAxiDma_LookupConfig(DEVICE_ID);
    XAxiDma_CfgInitialize(&dmaInstance, dmaConfig);

    setupTxRing(&txRing);
    setupRxRing(&rxRing);
    startTransfer();
    waitUntilDone();
    checkBuffers(txBuffer, rxBuffer);




    cleanup_platform();
    return 0;
}
